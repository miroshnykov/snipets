// import consola from 'consola';
// // import fs from 'fs';
//
// (async () => {
//   // fs.truncate('checkOfferIdProportional1.txt', 0, () => {
//   //   console.log('checkOfferIdProportional clean');
//   // });
//   const COUNT_CLICKS = 10;
//   consola.info(`processing ${COUNT_CLICKS} clicks ...`);
//   let success = 0;
//   let errors = 0;
//   const listOfferId = [];
//   const users = [
//     { id: 1, name: 'dimon' },
//     { id: 2, name: 'danik' },
//     { id: 3, name: 'katerina' },
//   ];
//   const tmpUser = users.map((u) => [u.id, u]);
//   const userById = Object.fromEntries(users.map((u) => [u.id, u]));
//   console.info(users);
//   console.info('tmpUser:', tmpUser);
//   console.info('userById:', userById);
//   console.info('userById:', userById[1]);
//
// })();
